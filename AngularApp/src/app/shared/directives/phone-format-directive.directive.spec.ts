import { PhoneFormatDirectiveDirective } from './phone-format-directive.directive';

describe('PhoneFormatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneFormatDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
