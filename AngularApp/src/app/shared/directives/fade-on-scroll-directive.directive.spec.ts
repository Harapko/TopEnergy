import { FadeOnScrollDirectiveDirective } from './fade-on-scroll-directive.directive';

describe('FadeOnScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOnScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
